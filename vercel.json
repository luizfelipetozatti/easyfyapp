{
  "buildCommand": "turbo build --filter=@easyfyapp/web",
  "outputDirectory": ".next",
  "installCommand": "pnpm install --frozen-lockfile",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 10
    }
  },
  "env": {
    "SKIP_ENV_VALIDATION": "true"
  }
}